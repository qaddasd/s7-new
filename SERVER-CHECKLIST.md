# ✅ Чеклист для исправления на сервере

## 🎯 Что сделать СЕЙЧАС на s7robotics.space

### Вариант 1: Быстрое исправление через nginx (5 минут)

```bash
# 1. Подключитесь к серверу
ssh user@s7robotics.space

# 2. Перейдите в директорию проекта
cd /path/to/s7-new

# 3. Скопируйте конфиг nginx
sudo cp nginx.conf.example /etc/nginx/sites-available/s7robotics.space

# 4. Создайте симлинк
sudo ln -sf /etc/nginx/sites-available/s7robotics.space /etc/nginx/sites-enabled/

# 5. Проверьте конфиг
sudo nginx -t

# 6. Перезагрузите nginx
sudo systemctl reload nginx

# 7. Проверьте что работает
curl https://s7robotics.space/api/health
# Должен вернуть: {"status":"ok"}
```

### Вариант 2: Полная автоматическая установка (10 минут)

```bash
# 1. Подключитесь к серверу
ssh user@s7robotics.space

# 2. Перейдите в директорию проекта
cd /path/to/s7-new

# 3. Обновите код
git pull

# 4. Запустите автоматическую установку
bash setup-production.sh

# 5. Настройте nginx (см. вариант 1 выше)
```

## 🔍 Проверка

После применения исправлений:

1. **Откройте в браузере:**
   ```
   https://s7robotics.space/check-api.html
   ```
   Нажмите "Запустить тесты" - все должны быть ✅ зелёными

2. **Или проверьте в консоли:**
   ```bash
   curl https://s7robotics.space/api/health
   curl http://localhost:4000/api/health
   ```
   Оба должны вернуть: `{"status":"ok"}`

3. **Откройте сайт:**
   ```
   https://s7robotics.space/dashboard
   ```
   Должен загрузиться дашборд, а не "Загрузка..."

## 🐛 Если не работает

### Проблема: nginx -t выдаёт ошибку

```bash
# Посмотрите где ошибка
sudo nginx -t

# Отредактируйте конфиг
sudo nano /etc/nginx/sites-available/s7robotics.space

# Проверьте что порты правильные (3000 и 4000)
```

### Проблема: backend не отвечает

```bash
# Проверьте что backend запущен
pm2 list
# Должен быть s7-backend в статусе "online"

# Посмотрите логи
pm2 logs s7-backend --lines 50

# Если не запущен - запустите
cd /path/to/s7-new/server
pm2 start npm --name "s7-backend" -- start
```

### Проблема: frontend не отвечает

```bash
# Проверьте что frontend запущен
pm2 list
# Должен быть s7-frontend в статусе "online"

# Посмотрите логи
pm2 logs s7-frontend --lines 50

# Если не запущен - запустите
cd /path/to/s7-new
pm2 start npm --name "s7-frontend" -- start
```

### Проблема: порты заняты

```bash
# Проверьте что слушает порты
sudo netstat -tlnp | grep :3000
sudo netstat -tlnp | grep :4000

# Если порты заняты другими процессами - остановите их
# или измените PORT в .env файлах
```

### Проблема: SSL сертификат не настроен

```bash
# Установите certbot
sudo apt install certbot python3-certbot-nginx

# Получите сертификат
sudo certbot --nginx -d s7robotics.space

# Автообновление сертификата
sudo certbot renew --dry-run
```

## 📊 Полезные команды

```bash
# Статус всех процессов
pm2 list

# Логи в реальном времени
pm2 logs

# Перезапуск всего
pm2 restart all

# Статус nginx
sudo systemctl status nginx

# Логи nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# Проверка портов
sudo netstat -tlnp | grep node
```

## 🎯 Итого

**После исправления у вас должно работать:**
- ✅ Frontend доступен на https://s7robotics.space
- ✅ Backend доступен на https://s7robotics.space/api/*
- ✅ Страницы загружаются нормально, нет "Загрузка..."
- ✅ API запросы работают
- ✅ SSL сертификат настроен

## 📞 Нужна помощь?

Если ничего не помогло, пришлите:
1. Скриншот браузера с ошибкой (F12 → Console и Network)
2. Вывод команд:
   ```bash
   pm2 list
   sudo nginx -t
   curl http://localhost:4000/api/health
   curl https://s7robotics.space/api/health
   ```
