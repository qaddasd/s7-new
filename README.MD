# 🚀 S7 Robotics Platform

Образовательная платформа для S7 Airlines с курсами, кружками, соревнованиями и мероприятиями.

## 🏗️ Технологический стек

### Frontend
- **Next.js 15** - React framework с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Lucide React** - Иконки

### Backend
- **Express.js** - REST API
- **Prisma** - ORM для работы с БД
- **PostgreSQL** - База данных
- **JWT** - Аутентификация

## 🚀 Быстрый старт (Development)

### 1. Клонирование и установка зависимостей

```bash
git clone <repository-url>
cd s7-new
npm install
cd server
npm install
cd ..
```

### 2. Настройка базы данных

Создайте файл `server/.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/s7robotics"
JWT_SECRET="your-secret-key-change-in-production"
PORT=4000
MEDIA_DIR="./media"
```

Примените миграции:
```bash
cd server
npx prisma migrate dev
npx prisma generate
cd ..
```

### 3. Запуск в dev режиме

```bash
# Терминал 1: Backend
cd server
npm run dev

# Терминал 2: Frontend
npm run dev
```

Откройте http://localhost:3000

## 🌐 Деплой на Production

### 🚨 ПРОБЛЕМА: "Не загружается на хосте"

Если приложение показывает "Загрузка..." на production, см. **[QUICK-FIX.md](./QUICK-FIX.md)**

### Автоматическая установка

```bash
bash setup-production.sh
```

### Ручная установка

1. **Установите зависимости и соберите проект:**
```bash
npm install
cd server && npm install && npm run build && cd ..
npm run build
```

2. **Настройте nginx** (см. [nginx.conf.example](./nginx.conf.example)):
```bash
sudo cp nginx.conf.example /etc/nginx/sites-available/s7robotics.space
sudo ln -s /etc/nginx/sites-available/s7robotics.space /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

3. **Запустите с PM2:**
```bash
# Backend
cd server
pm2 start npm --name "s7-backend" -- start

# Frontend
cd ..
pm2 start npm --name "s7-frontend" -- start

# Сохраните конфигурацию
pm2 save
pm2 startup
```

4. **Получите SSL сертификат:**
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d s7robotics.space
```

### Проверка работоспособности

Откройте в браузере: `https://s7robotics.space/check-api.html`

Или проверьте вручную:
```bash
curl http://localhost:4000/api/health
# Должен вернуть: {"status":"ok"}

curl https://s7robotics.space/api/health
# Тоже должен вернуть: {"status":"ok"}
```

## 📖 Документация

- **[QUICK-FIX.md](./QUICK-FIX.md)** - Быстрое исправление проблем с деплоем
- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - Подробная инструкция по деплою
- **[nginx.conf.example](./nginx.conf.example)** - Пример конфигурации nginx
- **[check-api.html](./check-api.html)** - Страница для диагностики API

## 🛠️ Полезные команды

### Development
```bash
npm run dev              # Запуск frontend в dev режиме
npm run build           # Сборка frontend
npm run start           # Запуск frontend в production режиме
cd server && npm run dev # Запуск backend в dev режиме
```

### Production (PM2)
```bash
pm2 list                # Список процессов
pm2 logs s7-backend     # Логи backend
pm2 logs s7-frontend    # Логи frontend
pm2 restart all         # Перезапуск всех процессов
pm2 stop all            # Остановка всех процессов
pm2 monit               # Мониторинг в реальном времени
```

### Database (Prisma)
```bash
cd server
npx prisma migrate dev   # Создать и применить миграцию
npx prisma generate      # Сгенерировать Prisma Client
npx prisma studio        # Открыть GUI для БД
npx prisma db seed       # Заполнить БД тестовыми данными
```

## 🏗️ Структура проекта

```
s7-new/
├── app/                    # Next.js App Router
│   ├── admin/             # Админ панель
│   ├── dashboard/         # Дашборд пользователя
│   ├── api/               # API routes (проксируют на Express)
│   └── ...
├── components/            # React компоненты
│   ├── tabs/             # Табы дашборда
│   ├── admin/            # Компоненты админки
│   └── ui/               # UI компоненты
├── server/                # Backend (Express + Prisma)
│   ├── src/
│   │   ├── routes/       # API роуты
│   │   ├── middleware/   # Middleware
│   │   └── index.ts      # Entry point
│   └── prisma/
│       └── schema.prisma # Database schema
├── styles/               # Глобальные стили
├── lib/                  # Утилиты
└── public/               # Статические файлы
```

## 🔐 Роли и права доступа

- **ADMIN** - Полный доступ ко всем функциям
- **STUDENT** - Доступ к курсам, кружкам, мероприятиям
- **MENTOR** - Управление кружками, проверка заданий

## ✨ Основные возможности

### Для студентов:
- 📚 Прохождение курсов с отслеживанием прогресса
- 🎓 Участие в кружках и мероприятиях
- 🏆 Соревнования и достижения
- 📊 Профиль с XP и статистикой

### Для менторов:
- 👥 Управление кружками и классами
- ✅ Проверка домашних заданий
- 📅 Ведение расписания и посещаемости
- 📤 Загрузка материалов

### Для администраторов:
- 🎨 Создание и редактирование курсов
- 🎮 Конструктор квизов
- 🏢 Управление кружками и мероприятиями
- 👤 Управление пользователями
- 📈 Аналитика

## 🐛 Отладка

### Проблемы с подключением к API на хосте

См. **[QUICK-FIX.md](./QUICK-FIX.md)**

### Проблемы с БД

```bash
cd server
npx prisma studio  # Откроется GUI для проверки данных
npx prisma validate # Проверка схемы
```

### Проблемы с авторизацией

Проверьте что в `server/.env` установлен `JWT_SECRET`

## 📝 TODO

- [ ] Unit тесты
- [ ] E2E тесты (Playwright)
- [ ] CI/CD pipeline
- [ ] Документация API (Swagger)
- [ ] Миграция на новый бэкенд
- [ ] Оптимизация производительности

## 📄 Лицензия

Proprietary - S7 Airlines

## 🤝 Контакты

При возникновении проблем создайте issue или свяжитесь с командой разработки.
